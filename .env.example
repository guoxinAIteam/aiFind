# ============ 上游：联通能开 AI 解析智能体 ============
# 严格对齐 docs/AIAgent.java；生产请用密钥管理替换
UNICOM_API_URL=https://ai-agent.chinaunicom.cn/uia/api/robot/chat/stream/v1
UNICOM_APP_ID=
UNICOM_TEAM_ID=
UNICOM_SK=
UNICOM_TIMEOUT_S=60

# 将 UNICOM_MOCK 设为 1 时，/api/parse 使用内置 mock 返回，便于前后端联调
UNICOM_MOCK=0

# ============ 下游：BDI 离线采集执行智能体 ============
BDI_BASE_URL=
BDI_KEY=
BDI_SKILL_NAME=接口规范-sftp-数据库-realease.skill
BDI_TIMEOUT_S=120
# 将 BDI_MOCK 设为 1 时，下游调用使用内置 mock
BDI_MOCK=0

# ============ 服务端口（可选；默认 8000 / 5173，被占用则顺延） ============
# PORT=8000
# VITE_PORT=5173
# BACKEND_PORT=8000
