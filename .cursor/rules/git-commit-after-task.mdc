---
description: 在本仓库每完成一项可交付任务后自动 git commit 并 push 到 GitHub
alwaysApply: true
---

# 任务完成后自动提交 GitHub

在本项目（aiFind）中，每当完成一项**独立、可交付**的开发或修复任务后（例如：修一个 bug、加一个接口、改完一页 UI），在回复用户之前必须执行：

1. **检查变更**：`git status`，只暂存与本次任务直接相关的文件；不要擅自 `git add` 大体积二进制、本地样例目录（如 `hbaseTest/`）、个人 `.docx` 等，除非用户明确要求纳入版本库。
2. **提交**：使用完整、可读的提交说明（中文或英文均可），说明「做了什么、为什么」。
3. **推送**：当前 `main` 分支跟踪的远程为 `org` 时，执行 `git push org main`；若跟踪关系变化，则对当前分支的上游执行 `git push`。
4. **失败处理**：若 push 因认证或网络失败，在回复中如实说明已本地提交及失败原因，并给出用户需在本机执行的命令，不要静默忽略。

若任务仅为只读说明、无需改代码，则不必制造空提交。
